---
type: handoff
created: 2026-01-19T22:28:36Z
updated: 2026-01-19T22:28:36Z
status: in_progress
project: docker-mcp
phase: Phase 1: Tool Additions + Scope Setup
summary: Docker MCP tool additions (6 tools) + master-mcp-server documentation improvements
next_action: Fix critical documentation issues OR continue with Phase 1 tool implementation
branch: main
task_type: enhance
---

# Handoff: Docker MCP + Master-MCP Documentation

## Session Summary

Completed major documentation improvements for master-mcp-server to address the gap: "How do I add new MCP servers and scopes?"

**Two workstreams:**
1. **docker-mcp**: Phase 1 - Add 6 new Docker tools (pending implementation)
2. **master-mcp-server**: Documentation structure created (needs fixes from team review)

---

## docker-mcp Work

### Current State

| Tool | Status | Notes |
|------|--------|-------|
| `pause_container` | Not started | Reuse `ContainerActionInput` |
| `unpause_container` | Not started | Reuse `ContainerActionInput` |
| `restart_container` | Not started | New schema with timeout |
| `tag_image` | Not started | New schema |
| `inspect_network` | Not started | New schema with include_attrs |
| `inspect_volume` | Not started | New schema with include_attrs |

### Task Plan

**Location:** `D:\servers\docker-mcp\docs\task_plan_tool_additions.md`

**Phase 1 - Input Schemas (Next):**
- Add `RestartContainerInput`, `TagImageInput`, `InspectNetworkInput`, `InspectVolumeInput`
- File: `src/mcp_server_docker/input_schemas.py`

**Key Decisions (from /team #deep):**
- Use `asyncio.to_thread()` for blocking Docker SDK calls
- Call `container.reload()` after mutations
- Docker-specific exceptions: `NotFound`, `APIError`, `ImageNotFound`
- Curated inspect output with `include_attrs` flag (default false)

---

## master-mcp-server Documentation Work

### Files Created

1. **D:\servers\master-mcp-server\CLAUDE.md**
   - Added "Quick Start: Adding Servers & Scopes" section
   - Clear distinction: config.json (repo root) vs config/default.json (template)

2. **D:\workspace\docs\getting-started\add-mcp-server.md**
   - Step-by-step server addition guide
   - Examples for Python (uv), Node (npx), Node (direct)
   - Verification steps

3. **D:\workspace\docs\getting-started\add-scope.md**
   - Step-by-step scope creation guide
   - Server vs Scope comparison
   - Per-project auto-scoping integration

4. **D:\servers\master-mcp-server\.ai\INSTRUCTIONS.md**
   - AI-first terse algorithmic instructions
   - Common patterns (Python, Node)
   - Verification commands

5. **D:\servers\master-mcp-server\config.reference.jsonc**
   - Annotated template with comments
   - Examples for each server type
   - Scope examples

### Critical Issues Found (Team #deep Review)

| Issue | File | Severity |
|-------|------|----------|
| Missing `master_oauth` block | config.reference.jsonc | HIGH |
| Wrong key: `callerProfiles` vs `callerFilters` | config.reference.jsonc | HIGH |
| Example missing `config` object | CLAUDE.md | HIGH |
| Too restrictive (only `"local"` type) | .ai/INSTRUCTIONS.md | MEDIUM |
| CORS security concern | config.reference.jsonc | MEDIUM |
| Wrong doc paths (workspace vs server repo) | CLAUDE.md, .ai/INSTRUCTIONS.md | LOW |

### Fixes Needed

The team identified issues that should be fixed before declaring this work complete. Priority fixes:

1. Add `master_oauth` block to config.reference.jsonc
2. Change `callerProfiles` → `callerFilters`
3. Add `config` object to CLAUDE.md example
4. Fix doc file path references

---

## Scope Setup for docker-mcp

### Current Status

The `docker` scope needs to be added to master-mcp-server:

**Location:** `D:\servers\master-mcp-server\config.json` (repo root)

**Add to `scopes{}` object:**
```json
"docker": {
  "description": "Docker container and image management",
  "servers": ["docker-mcp"],
  "aliases": []
}
```

**Verification:**
```javascript
list_scopes()  // Should show "docker"
enable_scopes(["docker"])
search_tools("docker")
```

---

## Docker MCP Test Results

**Working Tools:**
- `list_volumes` - 16 volumes found
- `list_networks` - 13 networks found
- `list_images` - 35+ images found
- `list_containers` - Working (transient pipe error once)

**Current Tool Count:** 19 tools

**After Phase 1:** 25 tools (+6 new tools)

---

## Next Actions

### Option A: Continue docker-mcp Phase 1
Implement the 6 new tools:
1. Add 4 input schemas to `input_schemas.py`
2. Add 2 output helpers to `output_schemas.py`
3. Register tools in `server.py`
4. Implement handlers with async threading
5. Test all 6 tools
6. Update documentation

### Option B: Fix Documentation Issues
Address the HIGH severity issues from team review:
1. Fix config.reference.jsonc (add master_oauth, fix callerFilters)
2. Fix CLAUDE.md example (add config object)
3. Fix .ai/INSTRUCTIONS.md (add other server types)
4. Fix doc path references
5. Re-review with team

### Option C: Complete Both
Fix documentation first, then continue with docker-mcp Phase 1.

---

## Key Files

| File | Purpose |
|------|---------|
| `D:\servers\docker-mcp\docs\task_plan_tool_additions.md` | Full implementation plan |
| `D:\servers\docker-mcp\CLAUDE.md` | Docker MCP project docs |
| `D:\servers\master-mcp-server\config.json` | Active config (add scope here) |
| `D:\workspace\docs\getting-started\add-mcp-server.md` | Server addition guide |
| `D:\workspace\docs\getting-started\add-scope.md` | Scope creation guide |

---

## Research Reference

Team review saved: `D:\workspace\docs\research\20260118_095221_MCP_Model_Context_Protocol_server_b.json`
